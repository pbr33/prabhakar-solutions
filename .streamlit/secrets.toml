# ── Microsoft Azure AD (SSO) ──────────────────────────────────────────
# Register an app in Azure AD → App registrations → New registration
# Redirect URI (Web): http://localhost:8501/  (or your deployment URL)
AAD_CLIENT_ID     = ""        # Application (client) ID
AAD_TENANT_ID     = ""        # Directory (tenant) ID
AAD_CLIENT_SECRET = ""        # Client secret value (optional for PKCE-less flow)
APP_BASE_URL      = "http://localhost:8501"

# ── Admin fallback ────────────────────────────────────────────────────
# Default admin: username = genaiwithprabhakar  password = ECI@2025!Presale
# To change the password, update _ADMIN_PASS_SHA in app1.py with:
#   python3 -c "import hashlib; print(hashlib.sha256(b'YOUR_PASSWORD').hexdigest())"
